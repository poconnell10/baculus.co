# STUB (M0): future historical backfill workflow.
#
# Intentionally NOT scheduled. There is no `on: schedule:` here — M0 does not
# activate any scheduled job. Manual dispatch only; performs no work. Documents
# the intended shape for a later milestone.
name: Backfill (stub — not scheduled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "Stub only. Backfill is deferred beyond M0."
