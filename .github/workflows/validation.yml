# STUB (M0): future scheduled validation workflow.
#
# Intentionally NOT scheduled. There is no `on: schedule:` here — M0 does not
# activate any scheduled job. Manual dispatch only; performs no work. Documents
# the intended shape for a later milestone.
name: Validation (stub — not scheduled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "Stub only. Validation is deferred beyond M0."
